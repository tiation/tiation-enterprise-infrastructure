apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-platform
  namespace: tiation
  labels:
    app: cms-platform
    domain: businesswebsite.live
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cms-platform
  template:
    metadata:
      labels:
        app: cms-platform
    spec:
      containers:
      - name: cms-platform
        image: nginx:alpine
        ports:
        - containerPort: 80
          name: http
        env:
        - name: DOMAIN
          value: "businesswebsite.live"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
        - name: cms-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: nginx-config
        configMap:
          name: cms-nginx-config
      - name: cms-content
        configMap:
          name: cms-html-content
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cms-nginx-config
  namespace: tiation
data:
  default.conf: |
    server {
        listen 80;
        server_name businesswebsite.live www.businesswebsite.live;
        root /usr/share/nginx/html;
        index index.html;
        
        location / {
            try_files $uri $uri/ /index.html;
        }
        
        # API endpoints (when CMS API is ready)
        location /api {
            proxy_pass http://cms-api-service:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        
        # Health check
        location /health {
            return 200 'CMS Platform OK';
            add_header Content-Type text/plain;
        }
    }
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cms-html-content
  namespace: tiation
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Business Website CMS - Coming Soon</title>
        <style>
            body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; text-align: center; }
            .container { padding: 100px 20px; max-width: 800px; margin: 0 auto; }
            h1 { font-size: 3em; margin-bottom: 20px; }
            p { font-size: 1.2em; margin-bottom: 30px; }
            .features { display: flex; justify-content: space-around; flex-wrap: wrap; margin: 50px 0; }
            .feature { background: rgba(255,255,255,0.1); padding: 20px; border-radius: 10px; margin: 10px; flex: 1; min-width: 200px; }
            .cta { background: #ff6b6b; color: white; padding: 15px 30px; border: none; border-radius: 5px; font-size: 1.1em; cursor: pointer; margin: 10px; }
            .cta:hover { background: #ff5252; }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🚀 Business Website CMS</h1>
            <p>Enterprise-grade headless CMS platform launching soon!</p>
            
            <div class="features">
                <div class="feature">
                    <h3>🎯 Headless CMS</h3>
                    <p>API-first content management</p>
                </div>
                <div class="feature">
                    <h3>⚡ Fast & Scalable</h3>
                    <p>Built for enterprise performance</p>
                </div>
                <div class="feature">
                    <h3>🔒 Secure</h3>
                    <p>Enterprise-grade security</p>
                </div>
            </div>
            
            <button class="cta" onclick="window.location.href='mailto:contact@businesswebsite.live'">Get Early Access</button>
            <button class="cta" onclick="window.location.href='https://tiation.online'">View Our Platform</button>
            
            <p style="margin-top: 50px; opacity: 0.8;">
                Part of the Tiation Enterprise Suite<br>
                <small>Platform Status: Launching Soon 🎉</small>
            </p>
        </div>
    </body>
    </html>
---
apiVersion: v1
kind: Service
metadata:
  name: cms-platform-service
  namespace: tiation
  labels:
    app: cms-platform
spec:
  selector:
    app: cms-platform
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cms-platform-ingress
  namespace: tiation
  annotations:
    kubernetes.io/ingress.global-static-ip-name: cms-ip
    networking.gke.io/managed-certificates: cms-platform-ssl-cert
    kubernetes.io/ingress.class: gce
spec:
  rules:
  - host: businesswebsite.live
    http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: cms-platform-service
            port:
              number: 80
  - host: www.businesswebsite.live
    http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: cms-platform-service
            port:
              number: 80
---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: cms-platform-ssl-cert
  namespace: tiation
spec:
  domains:
    - businesswebsite.live
    - www.businesswebsite.live